#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int amin(){
    int v[5][5];
    int i, j;
    srand(time(NULL));
    for(i = 0; i < 5; i++)
        for(j = 0; i < 5; j++)
            v[i][j] = rand() % 100;
    printf("Contingut de la matriu:\n");
    for(i = 0; i < 5; i++){
        for(j = 0; i < 5; j++)
            printf("%d", i,v[i][j]);
    printf("\n");
    }

    return 0;
}

